const elements = ['fire', 'air', 'water'];

const rta1 = elements.join(', ');
console.log({ rta1 });

// Urls amigables SEO
const title = 'Curso de manipulacion de arrays con js';
rta2 = title.split(' ').join('-').toLowerCase();
console.log({ rta2 });
